[phases.setup]
nixPkgs = ["nodejs"]
cmds = []

[phases.install]
cmds = ["npm exec --yes pnpm@10.30.1 -- install --frozen-lockfile"]

[phases.build]
cmds = ["npm exec --yes pnpm@10.30.1 -- run build"]

[start]
cmd = "node dist/index.js --transport=http"
